{
  "name": "connect4",
  "version": "1.0.0",
  "description": "jogo",
  "main": "src/index.js",
  "author": "allan braun",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "watch": "tsc --watch",
    "build": "tsc",
    "serve": "live-server --port=8080 --entry-file=./index.html",
    "serve-watch": "live-server --port=8080 --entry-file=./index.html & tsc --watch"
  },
  "dependencies": {
    "live-server": "^1.2.1",
    "tslint": "^6.1.3",
    "typescript": "^4.2.4"
  }
}
